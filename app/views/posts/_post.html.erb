<div id="<%= dom_id post %>">
  <div class="form-group">
    Title: <%= post.title %>
  </div>

  <div class="form-group">
    Description: <%= post.description %>
  </div>

  <% post.files.each do |file| %>
    <%= image_tag(file, width: 200, class: 'img-thumbnail') %>
  <% end %>
</div>
